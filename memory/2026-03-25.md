# 2026-03-25

- 用户补充课程笔记工作流要求：在原先标准不变的基础上，后续为 n1n 的 gpt-5.4 设计提示引导与限制时，要以飞书文档《课程1-中国的金融体系与债券市场》（docx/YAmMdI3aDoIBLnxBiSCcZntWnjc）为参考样例；先把“员工培训”做好，把去重、结构统一、表述收束、标签体系、重点取舍等要求写清楚，再要求稳定高质量产出。
- 已对“离线批处理 + 多模型课堂笔记”工作流做本地 smoke check：`offline_course_pipeline.ps1 -PlanOnly` 可跑通并生成 `upload_manifest.json`；`n1n_chat.py` 直连 `gpt-5.4` 可正常返回；`course_note_structure.ps1` 与 `course_note_audit.ps1` 能跑通；后续已修复 `course_note_rewrite.ps1` 与 `course_note_patch.ps1`：二者改为显式调用 `scripts\n1n_chat.py`，补上 Python 解释器路径解析，并清理了 `course_note_patch.ps1` 尾部残留脏行。修复后，rewrite/patch 单独 smoke 已通过；再用 `offline_course_pipeline.ps1` 对 `tmp\chapter2_first_video_test` 做小样本全链路回归（`-SkipTranscript -FrameInterval 1 -MaxFrames 1`）也已跑通，成功产出 `structured_outline.md`、`final_note_initial.md`、`quality_audit.md`、`final_note.md`、`upload_manifest.json`。补充：`tesseract.exe` 实际存在于 `C:\Program Files\Tesseract-OCR\tesseract.exe`，只是未进 PATH；当前脚本 fallback 可命中，因此 OCR 链路可用。
- 已补“最后一脚”加固：为 `video_batch_pipeline.py` 增加运行前预检（`frame_interval/max_frames` 合法性、ffmpeg/tesseract/curl/Whisper 依赖检查）以及“无抽帧结果”的明确报错；为 `n1n_chat.py` 增加 `TimeoutError/socket.timeout` 重试；为 `course_note_structure.ps1`、`course_note_rewrite.ps1`、`course_note_audit.ps1`、`course_note_patch.ps1` 统一补上 `--timeout 600`。随后用带转写的小样本链路继续回归：音频抽取、Whisper 本地转写、OCR、结构化、初稿重写均已通过；首次卡在 audit 超时，补强后从 audit 节点顺序重跑，成功产出 `quality_audit.fixed.md` 与 `final_note.fixed.md`，说明带转写场景下后半链路也已恢复。
- 本轮排障已做最小 git 留痕：先提交 `7f2004e`（`Fix course note rewrite/patch workflow scripts`），后提交 `40d5be7`（`Harden offline course-note pipeline timeouts and preflight`）。
- 用户要求将课堂笔记离线抽帧改为“纯规则自适应抽帧，不用模型”：常规 25 秒保底；大幅换页立即补帧；文本区域变化大立即补帧；表格/公式出现时短时提升到约 6 秒一帧；稳定后回归常规节奏。已先将 `scripts\video_batch_pipeline.py` 抽帧逻辑改为 `ffmpeg + tesseract + 规则合并`，不依赖 VLM，并新增 `frames\selection-meta.json` 记录每帧来源（baseline/scene/dense）。随后用户改为要求切换到 OpenCV 方案：已通过清华镜像安装 `opencv-python-headless`，把 `video_batch_pipeline.py` 调整为 **OpenCV 视觉规则优先，旧 OCR 规则兜底**；同时修复了 Windows 下 `cv2.imwrite` 中文路径写盘失败、Tesseract `TESSDATA_PREFIX` 缺失、以及中文路径 OCR 失败的问题。最终 smoke test 已通过，`selection-meta.json` 的 `mode` 已切换为 `opencv_visual_rules`，样本输出目录为 `tmp\adaptive-frame-smoke-opencv-v4`。
- 用户进一步明确课程笔记链路分工与控 token 方向：`结构提纯` 与 `审计` 模型应改为 `gemini-3.1-flash-lite-preview`；我在主会话中仅负责流程统筹、状态确认、飞书文档落地写入、归档、编号/排序识别，以及最小必要的飞书格式适配，不再在“落飞书”阶段做一轮大规模内容重写。该边界调整的目标之一是进一步降低 token 消耗。
