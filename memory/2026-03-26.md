# 2026-03-26

- 用户要求按顺序处理并安装以下技能：shill vetter、find-skills、proactive agent、skill-creator、ralph-loop；执行中已确认/处理的映射为：`openclaw-skill-vetter`、`openclaw-find-skills`（本地已存在）、`self-improving-proactive-agent`（本地已存在）、`skill-creator`、`ralph-loop-agent`。其中 `skill-creator` 与 `ralph-loop-agent`/`openclaw-find-skills` 在 ClawHub 中被标记为 suspicious，需要先审查再决定是否强装。
- 本轮已排查出 `memory_search` 不可用的两层原因：其一，原配置使用 `agents.defaults.memorySearch.provider = "openai"` + `text-embedding-3-small`，但当前环境并非稳定的可用 embeddings 配置；其二，切到本地 embeddings 后又发现本机缺少 `node-llama-cpp`，导致 local provider 也无法工作。已将 OpenClaw 的 memorySearch 配置临时改为 `provider=local`、`fallback=none`、`local.modelPath=hf:ggml-org/embeddinggemma-300M-GGUF/embeddinggemma-300M-Q8_0.gguf`，并执行过 `npm i -g openclaw@latest` 与 `npm i -g node-llama-cpp@latest` 继续修复，但当时尚未完成最终可用性闭环验证。
- 用户临时追问“最新版本工作流前半部分还有没有本地 Whisper 转写参与”时，已核对当前脚本并确认：最新离线课程工作流前半段默认**仍然包含本地 Whisper 转写**；`scripts\offline_course_pipeline.ps1/.py` 默认 `--stt-backend local`、本地模型默认 `medium`。只有显式 `--skip-transcript` 或手动切到 `--stt-backend api` 时，本地 Whisper 才不参与。
- 课程笔记离线工作流本轮已固化分工边界：`结构提纯`、`审计` 交给 `gemini-3.1-flash-lite-preview`；`成稿重写`、`最终修补` 交给 `gpt-5.4`；主会话只负责流程统筹、状态确认、飞书写入、归档编号识别和最小必要格式适配。参考飞书样例文档 `docx/YAmMdI3aDoIBLnxBiSCcZntWnjc` 已长期接入本地工作流，相关 guide / routing / config 已落地到 `references\course-note-reference\`、`scripts\course_note_reference.ps1`、`config\course_workflow_reference.json`。
- 针对“第2章第1条视频”的链路测试，本轮已按用户要求只从出问题节点往后重跑，不重跑完整链路；最终形成 `final_note_round6_finalcandidate.md` 作为可交付最终候选，并已新建飞书文档 `docx/K0qSdPGJCoZ9ToxFMrzcUtYAnPd`（标题：`第2章-中国的金融体系-最终候选`）完成写入。
- 用户最新明确要求：在飞书落稿阶段，助手需要重新承担“格式整理、结构收束、细节微调、飞书呈现适配”；不重做主结构，但要把现有稿收干净再覆盖回飞书。随后已按该要求把 `重点 / 高频考点 / 易错警示 / 了解低频` 统一改成标签头样式（如 `#### 【重点】`），并重新覆盖写回上述飞书文档。
- 审计机制本轮已继续降噪并以“可交付优先”为原则收紧：避免把标准简称、`1998通过/1999施行`、`Shibor 16/18 + 看题干年份口径`、以及紧凑笔记连接语误判为阻断问题；相关规则已继续沉淀到 `references\course-note-reference\05-audit-memory-and-delivery-threshold.md` 与 `scripts\course_note_audit.ps1`，并提交 git commit `19abd2f`（`Tighten audit pass threshold for final delivery`）。
- 本轮课程笔记测试已正式收口：用户已确认“最终版稿件”。当前落地飞书文档为 `docx/K0qSdPGJCoZ9ToxFMrzcUtYAnPd`；最终呈现采用“保留原正文结构，不把【重点】【高频考点】【易错警示】改成标题，而是整行加粗并突出对应内容”的方式。该类飞书收口应优先按“局部强调增强”处理，不要默认标题化。
- 用户新增长期要求：在测试流程结束后，要同步完成三件事——（1）把收口改动留存；（2）清理废弃残存垃圾、无效内容和遗留进程；（3）将课程笔记工作流模块化并完整封装为可复用 skill，确保后续更换课程仍可调用。
